# Make a list of numbers from one to one million
# Use min() and max() to make sure the list goes from 1 to 1000000
# Use the sum() function to see how quickly how Python can add a million numbers
numbers = list(range(1,1000001))
print(min(numbers))
print(max(numbers))
print(sum(numbers))