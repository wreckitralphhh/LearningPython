# Make a list of numbers from one to one million, and the use a for loop to print the numbers (if the output is taking too long stop it by pressing CTRL+C)
numbers = list(range(1,1000001))
for number in numbers:
    print(number)